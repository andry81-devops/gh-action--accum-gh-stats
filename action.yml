# CAUTION:
#   Not all features of a generic GitHub action is supported: https://github.com/actions/runner/issues/646
#
# **What does Composite Run Steps Not Support**
#
# We don't support setting conditionals, continue-on-error, timeout-minutes, "uses", and secrets on individual steps within a composite action right now.
#
# (Note: we do support these attributes being set in workflows for a step that uses a composite run steps action)
#

name: accum-gh-stats
description: 'GitHub composite action to request and accumulate a repository clones and/or views statistic'

inputs:
  deps_repo_owner:
    description: 'Owner of a repository used to checkout dependencies'
    required: true

  stat_repo_owner:
    description: 'Owner of a repository used to request the statistic'
    required: true

  stat_repo:
    description: 'Repository name used to request the statistic'
    required: true

  stat_entity_path:
    description: 'Statistic entity path to request'
    required: true
    # examples:
    # - traffic/clones
    # - traffic/views

  stat_repo_read_token:
    description: 'PAT token to a repository used to request the statistic with statistic read access rights'
    required: true

  stats_list_key:
    description: 'A list key containing GitHub repository statistic'
    required: true
    # examples:
    # - clones
    # - views

  curl_flags:
    description: 'additional curl flags'
    # example:
    #   `-H 'Cache-Control: no-cache'`

  output_repo_owner:
    description: 'Owner of a repository used to output the statistic'
    required: true

  output_repo:
    description: 'Repository name used to output the statistic'
    required: true

  output_repo_branch:
    description: 'Repository branch used to output the statistic'
    default: master

  output_repo_dir:
    description: 'Output repository relative directory path to store requested and accumulated statistic'
    required: true
    # examples:
    # - traffic/clones
    # - traffic/views

  output_repo_write_token:
    description: 'PAT token to the output repository with write/push access rights'
    required: true

runs:
  using: "composite"
  steps:
    # step is required to workaround variable expansion in variables
    - name: declare dependent variables
      shell: bash
      run: |
        echo "stats_dir=${{ inputs.output_repo_dir }}" >> $GITHUB_ENV
        echo "stats_json=${{ inputs.output_repo_dir }}/latest.json" >> $GITHUB_ENV
        echo "stats_list_key=${{ inputs.stats_list_key }}" >> $GITHUB_ENV

    - name: allocate directories
      shell: bash
      run: |
        mkdir $GITHUB_WORKSPACE/gh-workflow $GITHUB_WORKSPACE/gh-stats
        echo "WORKFLOW_TEMPDIR=$(mktemp -d)" >> $GITHUB_ENV

    - name: checkout gh-workflow
      uses: actions/checkout@v2
      with:
        repository: ${{ inputs.deps_repo_owner }}/gh-workflow
        path:       gh-workflow

    - name: checkout gh-stats
      uses: actions/checkout@v2
      with:
        repository: ${{ inputs.output_repo_owner }}/${{ inputs.output_repo }}
        ref:        ${{ inputs.output_repo_branch }}
        path:       gh-stats
        token:      ${{ inputs.output_repo_write_token }}

    - name: request ${{ inputs.stat_entity_path }} json
      shell: bash
      run: |
        curl --user "${{ inputs.stat_repo_owner }}:${{ inputs.stat_repo_read_token }}" \
          ${{ inputs.curl_flags }} \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/${{ inputs.stat_repo_owner }}/${{ inputs.stat_repo }}/${{ inputs.stat_entity_path }} \
          > "$GITHUB_WORKSPACE/gh-stats/$stats_json"

    # NOTE:
    #   Several features below is not supported in a composite action runnner:
    #     * conditionals
    #     * continue-on-error
    #

    - name: accumulate ${{ inputs.stat_entity_path }}
      id: accum-stats
      shell: bash
      run: |
        cd $GITHUB_WORKSPACE/gh-stats
        cat "$stats_json"
        chmod ug+x $GITHUB_WORKSPACE/gh-workflow/bash/github/accum-stats.sh
        $GITHUB_WORKSPACE/gh-workflow/bash/github/accum-stats.sh
        # caution: no operation after that line if non zero exit code happens above

    - name: generate messages
      shell: bash
      run: |
        echo "MINIDIFF_PRINT_MESSAGE=" >> $GITHUB_ENV
        echo "COMMIT_MESSAGE_SUFFIX=" >> $GITHUB_ENV
        if [[ -n "$STATS_UNIQUES_INC$STATS_COUNT_INC$STATS_UNIQUES_DEC$STATS_COUNT_DEC" ]]; then
          echo "MINIDIFF_PRINT_MESSAGE=unq all: +${STATS_UNIQUES_INC:-0} +${STATS_COUNT_INC:-0} / -${STATS_UNIQUES_DEC:-0} -${STATS_COUNT_DEC:-0}" >> $GITHUB_ENV
          echo "COMMIT_MESSAGE_SUFFIX= | unq all: +${STATS_UNIQUES_INC:-0} +${STATS_COUNT_INC:-0} / -${STATS_UNIQUES_DEC:-0} -${STATS_COUNT_DEC:-0}" >> $GITHUB_ENV
        fi

    - name: commit gh-stats
      shell: bash
      run: |
        echo "::notice ::diff: $MINIDIFF_PRINT_MESSAGE"
        cd $GITHUB_WORKSPACE/gh-stats
        git add .
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        git commit -m "Automated update: ${{ inputs.stat_entity_path }}$COMMIT_MESSAGE_SUFFIX"

    - name: push gh-stats
      uses: ad-m/github-push-action@master
      with:
        repository:   ${{ inputs.output_repo_owner }}/${{ inputs.output_repo }}
        branch:       ${{ inputs.output_repo_branch }}
        directory:    gh-stats
        github_token: ${{ inputs.output_repo_write_token }}
